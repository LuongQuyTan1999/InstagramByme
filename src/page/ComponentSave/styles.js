const styles = theme => ({
  container: {
    borderTop: theme.color.colorBorderTop
  },
  typography: {
    margin: "16px auto",
    display: "flex",
    justifyContent: "center",
    fontSize: "12px",
    color: "#999999"
  },
  typogra: {
    padding: "0px 1px"
  }
});

export default styles;
