const styles = theme => ({
  recomment: {
    height: "50px",
    padding: "15px",
    outline: "none",
    border: "none",
    width: "100%",
    borderTop: " 1px solid #DDD"
  }
});

export default styles;
